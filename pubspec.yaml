name: redtown
description: RedTown Reddit Media Scraper
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^6.2.1
  path: ^1.9.0

flutter:
  uses-material-design: true
